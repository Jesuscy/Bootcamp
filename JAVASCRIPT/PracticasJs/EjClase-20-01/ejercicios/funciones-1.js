/**
 * Escribir una función que reciba una lista por parámetro y devuelva una copia de la lista
 */
    const lista1 = [1,2,2]

    function listaCopy(lista){
        const listaCopy = lista;
        console.log(listaCopy);
        return listaCopy;
    }
     
    listaCopy(lista1); 